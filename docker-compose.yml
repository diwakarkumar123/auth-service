version: "3.8"

services:
  auth-service:
    build: .
    container_name: auth-service
    ports:
      - "5000:5000"
    env_file:
      - .env
    depends_on:
      - redis

  redis:
    image: redis:alpine
    container_name: redis
    ports:
      - "6379:6379"

